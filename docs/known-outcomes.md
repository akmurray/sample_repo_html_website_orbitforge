# Known Outcomes

- Products page always loads 4 fixture items from `public/assets/data/products.json`.
- Dashboard KPIs always render 4 static metrics.
- Changelog always loads 3 releases unless fixture is edited.
- Contact form requires both fields before success message appears.
