# Orbitforge Architecture

- `public/` contains static pages and assets.
- `assets/js/common` holds shared browser utilities.
- `assets/js/pages` contains page-scoped scripts.
- `scripts/serve.js` is a minimal local web server.
- `data/` contains deterministic fixtures for workflow checks.
