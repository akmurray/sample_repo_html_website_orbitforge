# Pipeline Exercise Backlog

Use this website as a target repo for autonomous or semi-autonomous workflows.

## Suggested exercises
1. Add a new nav item + page and keep active-route highlighting correct.
2. Add sorting controls to `products.html` and keep filtering behavior intact.
3. Convert dashboard KPI data to load from a JSON fixture.
4. Introduce a shared toast utility and use it on all pages.
5. Add form validation to a new contact page and include accessibility labels.
6. Refactor CSS into sections without changing rendered output.
7. Add a changelog page and wire it into nav.
8. Add deterministic smoke checks via a lightweight script.

## Expected workflow shape
- clone -> branch -> modify -> run local server -> verify behavior -> commit
